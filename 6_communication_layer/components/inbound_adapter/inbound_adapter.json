{
    "description":  "Receives external messages (HTTP, RF, serial) and normalizes them",
    "methods":  [
                    "normalize(message)",
                    "authenticate(source)"
                ]
}
