{
    "handle_user_goal":  [
                             "intent_parser.parse_goal()",
                             "task_orchestrator.decompose()",
                             "nervous_system.schedule_task()",
                             "monitor until completion and compile results"
                         ],
    "aggregate_cluster_outputs":  [
                                      "collect outputs",
                                      "apply conflict_resolver",
                                      "if unresolved -\u003e request introspection review"
                                  ]
}
